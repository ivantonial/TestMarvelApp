name: CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

env:
  SCHEME: 'MarvelApp'
  PROJECT_PATH: 'MarvelApp.xcodeproj'
  SECRETS_PATH: 'MarvelApp/Config'

jobs:
  build-and-test:
    name: Build and Test
    runs-on: macos-15

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Debug Scheme Configuration
      run: |
        echo "🔍 Looking for scheme files..."
        find ${{ env.PROJECT_PATH }} -name "*.xcscheme" -exec echo "Found: {}" \;
        
        SCHEME_FILE="${{ env.PROJECT_PATH }}/xcshareddata/xcschemes/${{ env.SCHEME }}.xcscheme"
        
        if [ -f "$SCHEME_FILE" ]; then
          echo ""
          echo "📄 Scheme file content:"
          cat "$SCHEME_FILE"
          
          echo ""
          echo "🔍 Checking for SDK references:"
          grep -i "sdk" "$SCHEME_FILE" || echo "No SDK references found"
          grep -i "26\." "$SCHEME_FILE" || echo "No 26.x references found"
        else
          echo "⚠️ Scheme file not found at expected location"
        fi

    - name: Select Latest Xcode
      run: |
        LATEST_XCODE=$(ls /Applications/ | grep "Xcode_" | sort -V | tail -1)
        if [ -z "$LATEST_XCODE" ]; then
          LATEST_XCODE="Xcode.app"
        fi
        sudo xcode-select -s /Applications/$LATEST_XCODE/Contents/Developer
        xcodebuild -version

    - name: Setup Secrets
      run: |
        cp ${{ env.SECRETS_PATH }}/Secrets-model.xcconfig ${{ env.SECRETS_PATH }}/Secrets.xcconfig
        sed -i '' 's/YOUR_PUBLIC_KEY_HERE/${{ secrets.MARVEL_PUBLIC_KEY }}/' ${{ env.SECRETS_PATH }}/Secrets.xcconfig
        sed -i '' 's/YOUR_PRIVATE_KEY_HERE/${{ secrets.MARVEL_PRIVATE_KEY }}/' ${{ env.SECRETS_PATH }}/Secrets.xcconfig

    - name: Cache SPM Dependencies
      uses: actions/cache@v4
      with:
        path: ~/Library/Developer/Xcode/DerivedData
        key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}

    - name: Build and Test
      run: |
        DEVICE_ID=$(xcrun simctl list devices "iOS 18.6" | grep "iPhone" | grep -oE "\([A-F0-9-]{36}\)" | head -1 | tr -d "()")
        
        if [ -z "$DEVICE_ID" ]; then
          echo "❌ No iOS 18.6 iPhone found!"
          exit 1
        fi
        
        xcrun simctl boot "$DEVICE_ID" || true
        sleep 5
        
        xcodebuild test \
          -project ${{ env.PROJECT_PATH }} \
          -scheme ${{ env.SCHEME }} \
          -destination "platform=iOS Simulator,id=$DEVICE_ID" \
          -enableCodeCoverage YES \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO